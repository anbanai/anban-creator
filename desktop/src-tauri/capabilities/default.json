{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Anban Creator desktop window. App-defined IPC commands (get_api_base, save_blob, is_local_executor_available, …) are invoke-able by default; the plugin permissions below cover any direct frontend use.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default"
  ]
}
