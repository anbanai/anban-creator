/.git/
/.idea/
/.vscode/
/.DS_Store
.env

# Local runtime data and release outputs.
/data/
/bin/
/release/
/dist/
/build/
/coverage/
*.log

# Frontend, desktop, and mobile sources that are not needed for
# building the server/agent Go binaries. plugins/ is intentionally kept
# because the Agent Dockerfiles copy it into the runtime image.
/studio/
/desktop/
/miniapp/
/wcflink/
/seednote-workspace/
/eval-article-viral-strategy/
/video-use-main/

# Dependency and cache directories can appear under subprojects.
**/node_modules/
**/dist/
**/build/
**/.cache/
**/.vite/
**/.next/
**/coverage/
