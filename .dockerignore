/.git/
**/.git
/.idea/
/.vscode/
/.DS_Store
.env

# Local runtime data and release outputs.
/data/
/bin/
/release/
/dist/
/build/
/coverage/
*.log

# Frontend and desktop sources that are not needed for
# building the Server or Agent runtime images. harness/ and agent-ts/ are
# intentionally kept because the Agent Dockerfiles copy them into the image.
/studio/
/desktop/
/wcflink/
/seednote-workspace/
/eval-article-viral-strategy/
/video-use-main/

# Dependency and cache directories can appear under subprojects.
**/node_modules/
**/dist/
**/build/
**/.cache/
**/.vite/
**/.next/
**/coverage/
