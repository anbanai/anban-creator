# Keep the build context lean and stage-1 COPY clean. dist is rebuilt inside
# the image, so a stale host dist must never leak into the build stage.
node_modules
dist
.git
.gitignore
*.log
*.md
.DS_Store
.vscode
.idea
coverage
