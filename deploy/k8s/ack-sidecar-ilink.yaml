apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sidecar-ilink-state
  namespace: "${namespace}"
  labels:
    app: sidecar-ilink
spec:
  storageClassName: nas-sc-creator
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "${sidecar_ilink_storage_size}"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sidecar-ilink
  namespace: "${namespace}"
  labels:
    app: sidecar-ilink
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: sidecar-ilink
  template:
    metadata:
      labels:
        app: sidecar-ilink
    spec:
      automountServiceAccountToken: false
      nodeSelector:
        kubernetes.io/arch: amd64
      imagePullSecrets:
        - name: "${imagePullSecret}"
      containers:
        - name: sidecar-ilink
          image: "${sidecar_ilink_image_repo}"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 18070
              protocol: TCP
          env:
            - name: WCFLINK_LISTEN_ADDR
              value: ":18070"
            - name: WCFLINK_STATE_DIR
              value: "/app/state"
            - name: WCFLINK_DB_PATH
              value: "/app/state/wcf.db"
            - name: WCFLINK_LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 18070
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health/live
              port: 18070
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: sidecar-ilink-state
              mountPath: /app/state
      volumes:
        - name: sidecar-ilink-state
          persistentVolumeClaim:
            claimName: sidecar-ilink-state
---
apiVersion: v1
kind: Service
metadata:
  name: sidecar-ilink
  namespace: "${namespace}"
  labels:
    app: sidecar-ilink
spec:
  type: ClusterIP
  selector:
    app: sidecar-ilink
  ports:
    - name: http
      port: 18070
      protocol: TCP
      targetPort: 18070
